/*
плюсы: 1. можно выбрать по какому столбцу делать объеденение
       2. объеденение может происходить по нескольким столбцам,
       3. не обязательно писать из какой таблицы если нужные столбцы есть только в одной таблице
минусы: 1. выйдет ошибка если у одинаково названных столбцов одинаковое имя но не тип данных
        2. может произойти cross join если нет одинаковых названий столбцов
факт: 1. по столбцам по которым происходит объеденение нельзя привязать к конкретной таблице
      2. нужно указывать из какой таблицы столбец если этот столбец есть во всех таблицах
*/
select * from  employees e join departments d using (department_id);


select * from regions
 join countries c using (region_id);
 
 select * from employees e  join job_history using (employee_id);
